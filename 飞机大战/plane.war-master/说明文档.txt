	1）、实现加载单个canvas，再绘制多个敌机图片和一个英雄图片，进行加载进去 。
  	2）、子弹，敌机，英雄的生成函数
  		-- 子弹、敌机和英雄的生成都采用了构造函数的方法进行绘制的，子弹和英雄增加了一个列表构造函数
		-- canvas：着重canvas绘图，业务逻辑全部在本地操作。
	3）、英雄与敌机的碰撞，敌机和子弹的碰撞。
 		--javascript：js算法设计碰撞的条件和碰撞后的结果。
总结：遇到的问题有子弹列表的构造函数一开始没有能够想到，在网上查找一些类型项目的资料，才得以解决，还有子弹碰撞的算法，子弹自动增加等一系列的算法都锻炼了自身的逻辑能力，熟悉掌握了HTML5新特性canvas一系列的素性和方法的使用，熟悉构造函数的创建，和创建的缘由。
